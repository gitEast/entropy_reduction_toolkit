<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alligator.mapper.Tool2Mapper">
    <select id="findAll" resultType="com.alligator.pojo.Tool2">
        SELECT * FROM tool2 ORDER BY create_at DESC;
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tool2 (date, weather, cause, thought, feeling)
        VALUES (#{date}, #{weather}, #{cause}, #{thought}, #{feeling})
    </insert>
</mapper>
